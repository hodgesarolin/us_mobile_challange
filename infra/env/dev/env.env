# Project Configuration
# CHANGE THESE VALUES FOR YOUR DEPLOYMENT

locals {
  # AWS Configuration - UPDATE THESE 2 VALUES
  aws_account_id  = "228490194431"  # CHANGE: Your AWS account ID
  gitops_repo_url = "https://github.com/hodgesarolin/us_mobile_challange"  # CHANGE: Your fork URL
  
  # Defaults (change if needed)
  aws_region      = "us-east-1"
  
  # Environment
  environment = "dev"
  cluster_name = "us-mobile-challenge-dev"
  
  # Domain configuration (optional)
  domain_base = ""
  
  # ECR repository (not used - images are on DockerHub)
  ecr_repo_name = "clippy-maze"
  
  # Node group configuration
  instance_type = "t3.medium"
  min_size      = 2
  max_size      = 4
  desired_size  = 2
  capacity_type = "SPOT"
}
